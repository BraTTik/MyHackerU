import React from 'react';
import Nav from '../../navbar';

const Home = (props) => {
    return(
        <div className="container-fluid" style={{padding: '0'}}>
            <Nav />
            header
            section(categories)
            section(top_goods)
            footer
        </div>
    )
}

export default Home;